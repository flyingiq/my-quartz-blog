name: Deploy Quartz to VPS`non: `n  push: `n    branches: [ v4 ] `njobs: `n  build-and-deploy: `n    runs-on: ubuntu-latest `n    steps: `n      - name: Checkout Code `n        uses: actions/checkout@v4 `n        with: `n          fetch-depth: 0 `n      - name: Setup Node `n        uses: actions/setup-node@v4 `n        with: `n          node-version: 24 `n          cache: "npm" `n      - name: Install Dependencies `n        run: npm ci `n      - name: Build Quartz `n        run: npx quartz build `n      - name: Deploy to Optimization Chicken `n        uses: easingthemes/ssh-deploy@main `n        env: `n          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }} `n          ARGS: "-avz --delete --no-perms --no-owner --no-group" `n          SOURCE: "public/" `n          REMOTE_HOST: "38.165.45.74" `n          REMOTE_USER: "root" `n          TARGET: "/var/www/quartz" `n          PORT: "2222"
